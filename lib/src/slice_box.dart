import 'package:flutter/widgets.dart';

class SliceBox extends StatefulWidget {
  SliceBox({Key key}) : super(key: key);

  @override
  _SliceBoxState createState() => _SliceBoxState();
}

class _SliceBoxState extends State<SliceBox> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}
